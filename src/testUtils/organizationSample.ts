import { Organization } from '../interfaces/types'
export default function organizationSample(): Organization[] {
    return [
        {
            id: 1,
            name: "Polus"
        }, {
            id: 2,
            name: "Google"
        }
    ]
}